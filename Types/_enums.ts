export enum QuestionType {
    single = "single",
    multiple = "multiple",
    short = "short",
    long = "long",
}

export enum ButtonType {
    primary = "primary",
    secondary = "secondary",
}

export enum LinkType {
    primary = "primary",
    secondary = "secondary",
}
